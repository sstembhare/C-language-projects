/* Multiplication of two numbers */
#include<stdio.h>
int main()
{
    int x=4, y=2, multiply;
    multiply = x * y;
    printf("\n value of x and y %d %d and Multiplication : %d", x, y, multiply);
}